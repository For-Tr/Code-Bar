[package]
name = "runtime-host"
version = "0.1.0"
edition = "2021"

[dependencies]
daemon-core = { path = "../daemon-core" }
codebar-contracts = { path = "../codebar-contracts" }
base64 = "0.22"
portable-pty = "0.8"
serde_json = "1"
